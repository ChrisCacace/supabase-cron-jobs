name: Push Test

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Push Test
        run: |
          echo "🚀 PUSH TRIGGERED WORKFLOW"
          echo "========================="
          echo "This workflow was triggered by a push to main"
          echo "Time: $(date -u)"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo ""
          echo "✅ If you see this, GitHub Actions is working!"
          echo "The issue is specifically with scheduled workflows." 